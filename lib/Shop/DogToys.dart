import 'package:flutter/material.dart';

class DogToys extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Dog Toys'),
      ),
      body: Center(
        child: Text('Dog Toys Page'),
      ),
    );
  }
}
