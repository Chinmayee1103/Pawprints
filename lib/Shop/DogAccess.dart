import 'package:flutter/material.dart';

class DogAccess extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Dog Accessories'),
      ),
      body: Center(
        child: Text('Dog Accessories Page'),
      ),
    );
  }
}
