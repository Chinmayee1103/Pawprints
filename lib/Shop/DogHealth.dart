import 'package:flutter/material.dart';

class DogHealth extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Dog Health'),
      ),
      body: Center(
        child: Text('Dog Health Page'),
      ),
    );
  }
}
